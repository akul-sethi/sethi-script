struct Cons(f, r) {
  var first = f;
  var rest = r;
}

var list = Cons(1, Cons(2, Cons(3, nil)));

def printList(list){
  if(list == nil) {
    return;
  }

  print list.first;
  printList(list.rest);
}

printList(list);
